# Installing project agents

If you already downloaded the agents zip (v3), extract it into the repo root so you get:
`.claude/agents/explorer.md`, `code-reviewer.md`, `test-runner.md`, `debugger.md`.

Verify:
- `ls -la .claude/agents`
- In Claude Code: `/agents`
- Or: `claude agents`
