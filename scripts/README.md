# Scripts

## make_handoff_packet.sh
Creates `tasks/handoff_packet.md` containing git status + diffs so you can switch between Claude and ChatGPT without losing context.

Usage (Git Bash):
```bash
bash scripts/make_handoff_packet.sh
```
