# Claude Prompt — Implement Spec (Token Budget + Plan Mode)

Token Budget Mode + Plan mode.

Implement: `docs/specs/<feature>.md`

Rules:
- Do NOT scan the whole repo.
- Read only the files required for this spec.
- Ask before running risky commands.
- Implement in small chunks.
- After implementation:
  - use `test-runner`
  - then `/verify` with evidence (commands + outputs)
- Before PR/merge:
  - use `code-reviewer` checklist

Start by:
1) Listing the exact files you will read.
2) Presenting the step-by-step plan.
Wait for approval before executing.
